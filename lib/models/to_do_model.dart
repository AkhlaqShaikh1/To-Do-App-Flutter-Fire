class ToDo {
  final bool isCompleted;
  final String title;
  final String description;

  ToDo({
    required this.isCompleted,
    required this.title,
    required this.description,
  });
}

List<ToDo> todo = [];
